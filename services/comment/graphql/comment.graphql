extend schema
   @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable"])

type Comment @key(fields: "id") {
  id: ID!
  content: String!
  authorId: String!
  postId: String!
  likeCount: Int!
}

type Query {
  comments: [Comment]
}
