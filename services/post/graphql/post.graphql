extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable"])

type Post @key(fields: "id"){
  id: ID!
  title: String!
  content: String!
}

type Query{
  posts: [Post]
}
