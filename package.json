{
  "name": "travelpedia_rnd",
  "type": "module",
  "private": true,
  "dependencies": {
    "@apollo/rover": "^0.37.0",
    "turbo": "^2.8.8"
  },
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "packageManager": "bun@1.3.5",
  "scripts": {
    "local": "turbo run local",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "build": "turbo run build",
    "generate": "turbo run generate",
    "supergraph": "rover supergraph compose --config ./supergraph.yaml --output ./dist/supergraph.graphql"
  }
}
